백엔드 팀에 요청

------------------------------------------------------------------------------

0번 리눅스 경로 문제

```
application.properties

# 현재
spring.servlet.multipart.location=/whguswo/upload
my.upload.path=/whguswo/upload

# 수정 요청
spring.servlet.multipart.location=./uploads
my.upload.path=./uploads
```
-------------------------

상영 스케줄 관련 
```
ScheduleServiceImpl

// 현재  — 런타임만으로 겹침 체크 → 정리시간 구간 뚫림
if (scheduleMapper.checkScheduleOverlap(..., endAt) > 0)

// 수정 요청 — 런타임 + 정리시간으로 체크
LocalDateTime occupancyEndAt = endAt.plusMinutes(theaterEntity.getCleanupTime());
if (scheduleMapper.checkScheduleOverlap(..., occupancyEndAt) > 0)
```
```
MovieManagePage.tsx
// previewEndTime = 시작 + 런타임 + 정리시간 ← 이미 cleanup 포함
const newEnd = timeToMin(previewEndTime)

// 기존 스케줄의 endTime도 DB에서 cleanup 포함된 값으로 저장돼 있음
const overlap = theaterDaySchedules.find((s) =>
newStart < timeToMin(s.endTime.slice(11, 16)) &&
newEnd   > timeToMin(s.startTime.slice(11, 16))
)
```
---------------------
결제 로그에
ORDER BY create_at DESC 적용하면 좋을 듯. 
프론트에서도 적용해두긴 했으나, 받아온 페이지(10건)안의 데이터 안에서만 적용됨. 
근본적인 정렬은 백에서 해야함. 현재는 id 순으로 정렬되고 있음. 

