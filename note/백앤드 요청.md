백엔드 팀에 요청

### ScheduleMapper.xml      

s.activation 누락     electOneById, selectAll에 각각 추가

----------------------------------------------------------------

### PaymentController

포인트 잔액 갱신       PaymentController L210 주석처리됨    member.setPoint(...) 주석 해제


 savePaymentInfo 메서드 안에

// 문제 코드 — couponNum이 빈 문자열이면 DB에서 못 찾아서 예외 발생
CouponDTO couponNum = discountPolicyService.getCoupon(requestData.path("couponNum").asText(""));

아래처럼 수정
String couponNumStr = requestData.path("couponNum").asText("");
CouponDTO couponNum = couponNumStr.isEmpty() ? null : discountPolicyService.getCoupon(couponNumStr);


                .couponNum(couponNum) 주석 해제
                .couponNum(null) 주석

---------------------------------------------------------------------------

### CustomerController

 POST /api/coupon/auth 반환 타입 변경

// 현재
public ResponseEntity<Boolean> authCoupon(@RequestParam String couponNum) {
return ResponseEntity.ok(discountPolicyService.authCoupon(couponNum));
}

// 변경 후 (discountPolicyService.getCoupon 추가 조회)
public ResponseEntity<CouponDTO> authCoupon(@RequestParam String couponNum) {
discountPolicyService.authCoupon(couponNum); // 검증 (예외 그대로)
return ResponseEntity.ok(discountPolicyService.getCoupon(couponNum)); // 상세 반환
}
------------------------------------------------------------------------------